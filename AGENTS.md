# AGENTS.md — CallGuard 하네스 운영 규칙

> 이 저장소는 단독 운영자 + 로컬 LLM 에이전트 하네스로 개발된다.
> 에이전트는 매 세션 이 문서를 처음부터 끝까지 읽고, 여기 정의된 루프만 수행한다.
> 우선순위: **AGENTS.md 가드레일 > PRD.md > plan.md > 코드 주석.** 충돌 시 상위 문서를 따르고, 해석이 필요하면 STOP(§3).
> 로컬 모델 전제: 컨텍스트가 짧고 추론이 불완전할 수 있다. 그래서 이 하네스의 원칙은 **"작게 작업하고, 스크립트로 검증하고, 파일에 상태를 남긴다"** — 에이전트의 기억이나 자기 평가를 신뢰 근거로 삼지 않는다.

---

## 1. 세션 루프 (모든 세션은 이 순서를 벗어나지 않는다)

```
1. READ    — AGENTS.md 전체 → STATE.md → plan.md에서 STATE.md가 가리키는 현재 태스크 1개
2. VERIFY  — git status 확인(더러운 트리면 먼저 정리·보고), 직전 태스크의 DoD 스크립트 재실행으로 기반 확인
3. PLAN    — 현재 태스크를 30분 내 완료 가능한 변경 단위로 쪼갠 체크리스트를 STATE.md의 "작업 메모"에 기록
4. WORK    — 체크리스트 1개 항목씩: 구현 → 테스트 작성 → 로컬 실행
5. CHECK   — 태스크의 DoD 명령을 실행. 출력 원문을 STATE.md에 붙여넣는다 (요약 금지)
6. COMMIT  — DoD 통과 시에만 커밋 (규칙 §5). 실패 시 커밋하지 않고 실패 내용을 STATE.md에 기록
7. UPDATE  — STATE.md 갱신: 완료 태스크 체크, 다음 태스크 포인터 이동, 미해결 이슈 기록
8. STOP    — 세션 종료. 다음 태스크를 임의로 시작하지 않는다 (1 세션 = 1 태스크가 기본)
```

- 한 세션에서 태스크를 끝내지 못하면: 진행분을 WIP 커밋(`wip(scope): ...`)으로 남기고 STATE.md에 "이어서 할 일"을 다음 세션의 자신이 맥락 없이 읽어도 되게 명령형으로 기록한다.
- **컨텍스트가 부족하면 코드를 추측으로 쓰지 말고**, 필요한 파일 경로를 STATE.md에 적고 세션을 끝낸다.

## 2. STATE.md 규약 (하네스의 단일 상태 파일)

STATE.md는 저장소 루트에 있고, 형식은 고정이다:

```markdown
# STATE
## 현재 태스크
P2-T3   <- plan.md의 태스크 ID 하나만
## 완료 태스크
- [x] P1-T1 (2026-08-12, commit abc1234)
...
## 작업 메모 (현재 태스크의 세부 체크리스트)
- [x] TranscriptSegment 타입 정의
- [ ] remote 트랙 스트림 연결
## 마지막 DoD 실행 결과 (원문)
$ swift test --filter STTTests
... (출력 그대로)
## 미해결 이슈 / 다음 세션 지시
- whisper.cpp 빌드 시 Metal 플래그 확인 필요: docs/stt-notes.md 참조
## STOP 보고 (운영자 확인 대기)
- (없음)
```

- 에이전트는 STATE.md 이외의 곳에 진행 상태를 기록하지 않는다(중복 상태 금지).
- STATE.md는 매 세션 종료 시 반드시 커밋에 포함한다.

## 3. STOP 조건 — 즉시 작업을 멈추고 STATE.md의 "STOP 보고"에 기록 후 세션 종료

다음 상황에서 에이전트는 스스로 결정하지 않는다:

1. plan.md의 게이트 태스크(G-1, G-2, G-3) 판정 — 판정은 운영자가 한다. 에이전트는 측정 결과만 준비한다.
2. R1 검증 실패 등 폴백 경로 전환이 필요한 경우 — BlackHole 등 폴백을 임의 착수 금지.
3. 새 의존성 추가가 필요하다고 판단한 경우 (§6-G10).
4. PRD의 Could 항목 또는 스코프 밖 작업이 필요해 보이는 경우.
5. DoD 스크립트가 존재하지 않거나 실행 불가한 경우 — 스크립트를 임의로 "통과한 셈" 치지 않는다.
6. 가드레일과 태스크 지시가 충돌하는 경우.
7. 동일 오류로 3회 연속 수정 시도가 실패한 경우 — 4번째 시도 대신 STOP (루프 낭비 방지).

STOP 보고 형식: `[태스크 ID] 무엇을 하려 했나 / 무엇이 막혔나(오류 원문) / 에이전트가 검토한 선택지 / 운영자에게 필요한 결정`.

## 4. 아키텍처·기술 스택 (변경 금지 사항)

데이터 흐름 (모듈 의존 방향도 이 순서만 허용, UI→Capture 직접 참조 금지):

```
FileAudioSource(파일 재생, MVP 주경로) ─┐
SCK(remote)/Mic(local) [Could, F-C4]  ─┤→ Session(트랙 태깅) → Preprocess(16kHz, VAD, 2s 청크)
                                                  → STT(whisper.cpp, 트랙별 인스턴스)
                                                  → Detection(RuleEngine → 경량 선형 분류기, 60s 윈도)
                                                  → AlertPolicy(0.5 주의 / 0.8 위험, 히스테리시스)
                                                  → UI(메뉴바/배너/전면 패널)
                                                  → SessionStore(기본 폐기, 옵트인 암호화 저장)
```

> **2026-08-10 스코프 변경(운영자)**: MVP 입력은 `AudioSource`(FileAudioSource)를 통한 WAV 재생이 주 경로다. SCK/Continuity 실기기 캡처는 Could(PRD F-C4)로 격하 — 관련 태스크는 plan.md Phase 1 참조(보류 상태로 유지, 재작업 금지). 상세 근거는 PRD.md §2·§6(R1) 참조.

| 영역 | 확정 스택 | 에이전트가 하지 말 것 |
|---|---|---|
| 앱 | Swift 5.10+ / SwiftUI / SwiftPM | CocoaPods, Obj-C 신규 코드 |
| 캡처(MVP) | `AudioSource`(FileAudioSource, 실시간 페이싱) 파일 재생이 주 경로 | 비디오 캡처 활성화, BlackHole 임의 도입, 라이브 SCK/Continuity 추가 투자(운영자 승인 없이 — Could 격하 상태) |
| STT | whisper.cpp (Metal, 모델 해시 고정) | 클라우드 STT, 모델 파일 무단 교체 |
| 분류 | 경량 선형 분류기(TF-IDF+로지스틱회귀) → 가중치 JSON, Swift 인프로세스 직접 구현 | Python 사이드카, 런타임 pip 의존, CoreML/BERT 재도입(운영자 승인 없이) |
| ML 툴링 | Python 3.11, `ml/` 격리 | `ml/` 코드·의존성의 `Sources/` 유입 |
| 저장 | GRDB(SQLite) + 필드 암호화 | 평문 저장, 외부 DB |
| 테스트 주입 | `AudioSource` 프로토콜 + `FileAudioSource`(실시간 페이싱) | SCK를 목 없이 테스트에 직접 사용 |

디렉토리 구조는 저장소의 실제 트리를 따른다 — 새 최상위 디렉토리 생성은 STOP 대상.

## 5. 코딩·커밋 규칙

- Swift: swiftformat+swiftlint 설정 준수(위반 시 커밋 전 수정). `async/await`+`AsyncStream`만, `DispatchQueue` 직접 사용 금지(오디오 콜백 경계 제외). 강제 언래핑 `!` 금지, `try!`/`try?` 금지. 파이프라인 타입은 `Sendable` 불변 struct.
- 커밋: Conventional Commits `feat|fix|refactor|test|docs|chore|wip(scope): 요약 (태스크ID)`. 예: `feat(stt): 트랙별 스트리밍 인스턴스 (P2-T3)`.
- 1 커밋 = 1 논리 변경. 포맷팅 변경을 기능 커밋에 섞지 않는다.
- 에러 핸들링: 모듈별 typed error. **조용한 실패 금지** — 스트림 단절 시 5초 내 재시도, 실패 시 "보호 중단됨" 상태 이벤트 발행. 로그에는 원인·단계·타임스탬프만(§6-G1).
- 모든 기능 커밋에는 테스트가 동반된다. 실패 조건을 실제로 검증하지 않는 빈 테스트 금지.

## 6. Guardrails (위반 = 해당 커밋 폐기 대상)

- **G1** 통화 오디오·전사·근거 발화를 로그/콘솔/임시파일에 평문 출력하는 코드 금지. 디버깅은 길이·해시·타임스탬프만. `#if DEBUG` 뒤라도 전사 원문 출력은 금지.
- **G2** 오디오/전사/판정을 전송하는 네트워크 코드 금지. 앱 타깃에 `URLSession` 아웃바운드 추가 금지. 클라우드 STT/LLM 호출 금지(F-W3).
- **G3** 키·시크릿 하드코딩 금지. (이 아키텍처에 외부 API 키는 존재할 이유가 없다 — 필요해 보이면 설계 오류이므로 STOP.)
- **G4** iOS/Android 통화 접근, 사설 API, 통화 앱 후킹 코드 금지.
- **G5** 실제 인물 통화 녹음·전사를 저장소에 커밋 금지. 픽스처는 합성(TTS/낭독)·공개 데이터 유래만, 출처 주석 필수. `ml/datasets/`는 .gitignore 유지.
- **G6** SCStream 비디오/화면 캡처 활성화 금지.
- **G7** 무저장 기본값·동의 게이트·암호화를 약화하는 변경 금지. 동의 전 파이프라인이 시작되는 코드 경로 금지.
- **G8** 자동 차단/자동 신고/외부 기관 연동 금지(F-W1).
- **G9** 지연 예산(PRD §5) 초과가 예상되는 구현은 도입 전 STOP.
- **G10** 의존성 추가는 STOP 대상. 운영자 승인 후에만, lockfile 갱신과 함께.
- **G11** entitlements/Info.plist/서명 설정 변경은 단독 커밋으로 분리하고 사유 명시.
- **G12 (하네스 전용)** 에이전트는 DoD 스크립트·평가 스크립트·이 문서(AGENTS.md)·PRD.md를 수정하지 않는다. 테스트를 통과시키기 위해 테스트나 임계값을 고치는 행위 금지. 스크립트 버그로 보이면 STOP 보고.
- **G13 (하네스 전용)** "달성했다"는 서술로 DoD를 대체 금지. 판정 근거는 오직 스크립트 실행 출력 원문(STATE.md 기록)이다.

## 7. 검증·측정 규칙

- 지표(M1–M7, M9)는 PRD §3의 지정 스크립트로만 측정한다. 스크립트가 미구현이면 해당 태스크(plan.md P0-T4 등)가 선행이다.
- `FileAudioSource`는 벽시계 실시간 페이싱 필수 — 일괄 방출 구현은 지연 측정을 무효화하므로 금지.
- 빠른 테스트(단위+룰/정책)와 느린 테스트(STT 통합, E2E, 부하)는 태그로 분리한다. 세션 중 기본은 빠른 테스트, DoD가 요구할 때만 느린 테스트.
- 실기기 검증(실통화 스모크)은 운영자 수행 항목이다. 에이전트는 체크리스트(docs/manual-smoke.md)를 최신으로 유지하는 것까지만 한다.

## 8. 판단 기준 (모호할 때, STOP 전에 이 순서로 자기 점검)

1. 프라이버시(G1/G2/G7) 위반 소지가 있으면 → 무조건 하지 않는다.
2. 성능 vs 정확도 → 지연 예산(M1) 먼저, 그 안에서 정확도.
3. 정확도 내부 → Recall 우선하되 FPR ≤ 5%를 깨는 튜닝 금지.
4. Must > Should. Could는 운영자 승인 전 착수 금지.
5. 라이브러리 추가 vs 직접 구현 30줄 → 직접 구현.
6. 그래도 불확실 → STOP. 추측 구현은 이 하네스에서 가장 비싼 실패다.

## 9. 참고 문서 (필요 태스크에서만 조회)

STT: ggml-org/whisper.cpp README·stream 예제 · 분류: scikit-learn(TF-IDF+로지스틱회귀, `ml/` 격리 Python 툴링) — 가중치는 JSON export 후 Swift에서 직접 파싱 · 참고 데이터/코드: 금감원 "그놈 목소리"(이용조건 확인 후), KorCCVi 데이터셋(라이선스 확인) · Could 재개용: SCK *ScreenCaptureKit* (`SCStreamConfiguration.capturesAudio`) · 마이크 *AVAudioEngine* input tap · 내부: `docs/PRD-full.md`, `docs/` 스파이크 리포트.
API 사용 전 반드시 해당 심볼이 실재하는지 문서/헤더로 확인한다 — 존재하지 않는 파라미터를 지어내는 것은 이 하네스에서 가장 흔한 실패 모드다.
